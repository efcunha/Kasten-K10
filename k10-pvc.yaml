apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: teste-pvc
   namespace: kasten-io
spec:
   storageClassName: managed-nfs-storage
   accessModes:
      - ReadWriteMany
   resources:
      requests:
         storage: 200Gi
